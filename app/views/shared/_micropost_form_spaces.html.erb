<%= form_for @micropost do |f| %>
<%= f.hidden_field :space_id, :value => id %>
<%= f.hidden_field :building_id, :value => @building.id %>
<%= hidden_field_tag "back_to", building_space_path(@space) %>

<%= f.text_area :content, :size => "50x5"%>
<%= f.submit "Submit" %>

<% end %>
