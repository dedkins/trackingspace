<% if user_signed_in? %>

	<% unless @recent_items.empty? %>
		<table width="100%">
			<tr>
				<td class="listbox_title">Recently Visited</td>
			</tr>
			<tr>
				<td ><%= render :partial => 'shared/recent_item', :collection => @recent_items %></td>
			</tr>
		</table>
	<% end %>
	
<% end %>