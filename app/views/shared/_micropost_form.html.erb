<%= form_for @micropost do |f| %>
<% if @building.present? %>
<%= f.hidden_field :building_id, :value => @building.id %>
<% end %>
<%= f.text_area :content, :size => "50x5"%>
<%= f.submit "Submit" %>

<% end %>
