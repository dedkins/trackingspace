<table>
	<h1><%= @building.address %> | Avg: <%= render 'avg_rate' %></h1>
	
	<% if mobile_device? %>
	<tr>
		<%= gmaps({
		  "map_options" => {"auto_zoom" => false, "zoom" => 18, "type" => 'SATELLITE' },
		  "markers" => {"data" => @json }
		})
		%>
	</tr>
	<tr width="100%" align="justify" height="75px">
		<% if user_signed_in? %>
		<td><div id="greenbutton">Add Comment</div></td>
		<td><div id="greenbutton">See Spaces</div></td>
		<td><div id="greenbutton">Edit Building</div></td>
		<% end %>
		<% unless user_signed_in? %>
		<div id="announcement"><%= link_to 'Click Here to Add Comments or to Update the Building/Spaces', '/auth/twitter' %></div>
		<% end %>
	</tr>
	<tr width="100%" valign=top>
		<% unless @building.microposts.empty? %>
			<%= render 'shared/feed' %>
		<% end %>
	</tr>

	<% end %>

	<% if not mobile_device? %>
	<tr>
		<td width="600px" valign=top >
		<% if user_signed_in? %>
		<h3>Add a comment about this building:</h3>
			<%= render '/shared/micropost_form' %>
		<% end %>
		<% unless @building.microposts.empty? %>
			<%= render 'shared/feed' %>
		<% end %>
	</td>
	
	
	<td width="20px">
	</td>
	<td width="300px" valign=top>
		<%= gmaps({
		  "map_options" => {"auto_zoom" => false, "zoom" => 18, "type" => 'SATELLITE' },
		  "markers" => {"data" => @json }
		})
		%>
		<hr>
		<% if @building.spaces.any? %>
		Spaces
		<%= render 'spaces' %>
		<%= link_to 'Add New Space', new_building_space_path(@building) %>
		<% end %>


		<%= link_to 'Edit', edit_building_path(@building) %> |
		<%= link_to 'Back', buildings_path %>

	</td>
	<% end %>
</tr>
<%= yield :scripts %>
<% if user_signed_in? %>
			<%= render '/shared/micropost_form' %>
		<% end %>
</table>