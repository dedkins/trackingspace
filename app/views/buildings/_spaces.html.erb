<% if @spaces %>
<table>
  <tr>
    <th>Suite #</th>
    <th>Sf</th>
    <th>Monthly Cost</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @spaces.each do |space| %>
  <tr>
    <td><%= space.suite %></td>
    <td><%= space.sf %></td>
    <td><%= space.monthly %></td>
    <td><%= link_to 'Show', building_space_path(@building,space) %></td>
    <td><%= link_to 'Edit', edit_building_space_path(@building,space) %></td>
  </tr>
<% end %>
</table>
<% else %>
No Spaces
<% end %>