<%= currently_at 'Leases' %>

<% if @lease.user_id = current_user %>

<%= render 'leases/pdf' %>

<% content_for :sidebar do %>


	<table width="100%">
		<tr class="tabletitle">
			<th colspan="3">Stats</th>
		</tr>
		<tr>
			<td width="10px"/>
			<td width="20px">Lease</td>
			<td><%= link_to 'Download Copy', @lease.file.url %></td>
		</tr>
		<tr>
			<td width="10px"/>
			<td>Size</td>
			<td><%= @lease.size %></td>
		</tr>
		<tr>
			<td width="10px"/>
			<td>Current Monthly</td>
			<td><%= number_to_currency(@lease.current_rate) %></td>
		</tr>
		<tr>
			<td width="10px"/>
			<td>Expiration</td>
			<td><%= @lease.expiration %></td>
		</tr>
	</table>
	<hr/>
	<table width="100%">
		<tr>
			<td style="text-align:center">
				<%= link_to 'Edit', edit_space_lease_path(@lease) %> |
				<%= link_to 'Back', space_leases_path %>
			</td>
		</tr>
	</table>
<% end %>

<% else %>

Oops.  Somehow we ended up here.  To look at this lease, you need to either have created it, or had it shared with you.  
	

<% end %>
