<%= currently_at 'Spaces' %>

<% if @lease.user_id = current_user %>

<table width="100%">
	<tr>
		<td colspan="3" style="text-align:center;font-size:.9em">
				<%= link_to 'Edit', edit_space_lease_path(@lease) %> |
				<%= link_to 'Download Copy', @lease.file.url %> |
				<%= link_to 'See Space', space_lease_path(@lease) %>
		</td>
	</tr>
	<tr height="12px"/>
	<tr>
		<td style="font-size: 2em;text-align:center" colspan="3">L  E  A  S  E</td>
	</tr>
	<tr height="12px" style="font-size:.8em;text-align:center">
		<td colspan="3" ><%= @lease.space.building.address %>, Suite: <%= @lease.space.suite %></td>
	</tr>
	<tr>
		<td colspan="3"><hr/></td>
	</tr>
	<tr>
		<td width="33%" class="top_words"><%= number_with_delimiter(@lease.size) %>sf</td>
		<td width="33%" class="top_words"><%= number_to_currency(@lease.current_rate) %></td>
		<td width="33%" class="top_words"><%= l @lease.expiration %></td>
	</tr>
	<tr>
		<td style="text-align:center">Size</td>
		<td style="text-align:center">Current Monthly</td>
		<td style="text-align:center">Expiration</td>
	</tr>
	<tr>
		<td colspan="3"><hr/></td>
	</tr>
</table>


<%= render 'leases/pdf' %>

<% content_for :sidebar do %>

<% end %>

<% else %>

Oops.  Somehow we ended up here.  To look at this lease, you need to either have created it, or had it shared with you.  
	

<% end %>
