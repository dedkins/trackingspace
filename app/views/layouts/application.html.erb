<!DOCTYPE html>
<html>
<head>
  <title>Trackingspace</title>
  
  <script src="http://maps.google.com/maps/api/js?sensor=false"></script>

  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  
    <script>
        	$(function() {
        		var addresspicker = $( "#addresspicker" ).addresspicker();
        		var addresspickerMap = $( "#addresspicker_map" ).addresspicker({
        			regionBias: "fr",
        		  elements: {
        		    map:      "#map",
        		    lat:      "#lat",
        		    lng:      "#lng"
        		  }
        		});
        		var gmarker = addresspickerMap.addresspicker( "marker");
        		gmarker.setVisible(true);
        		addresspickerMap.addresspicker( "updatePosition");
        
        	});
  	</script>
</head>
<body>
    <div id="header">
      <div id="logo">
        <%= link_to 'TrackingSpace:', buildings_path %><br/><%= render 'buildings/form_new' %>
      </div>
      <div id="user_nav">
        <% if user_signed_in? %>
          Signed in as <%= current_user.email %>. Not You?
          <%= link_to "Sign Out", destroy_user_session_path, :method => 'delete' %>
        <% else %>
          <%= link_to "Sign Up", new_user_registration_path %> or <%= link_to "Sign In", authentications_path %>
        <% end %>
        <br/><br/>
        <%= link_to "Buildings", buildings_path %> | <%= link_to "Spaces", buildings_path %> | <%= link_to "People", buildings_path %> | <%= link_to "Profile", buildings_path %>
      </div>
    </div>
  
    <% flash.each do |name, msg| %>
      <% content_tag :div, msg, :id => "flash_#{name}" %>
    <% end %>
    <div id="yield">
      <%= yield %>
      <%= debug(params) if Rails.env.development? %>
    </div>
</body>
</html>
